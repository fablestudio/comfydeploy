{
  "name": "comfydeploy",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run docker-compose dev tunnel --parallel"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.10",
  "workspaces": [
    "apps/*"
  ]
}
